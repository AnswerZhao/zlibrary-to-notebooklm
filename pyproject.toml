[project]
name = "zlibrary-to-notebooklm"
version = "1.0.0"
description = "Automatically download books from Z-Library and upload to Google NotebookLM"
requires-python = ">=3.10"
dependencies = [
    "playwright>=1.40.0",
    "ebooklib>=0.18",
    "beautifulsoup4>=4.11.0",
    "lxml>=4.9.0",
]

[tool.uv]
dev-dependencies = []
